# Base URL for the website (used in sitemap and robots.txt)
# Leave blank to use the default: https://www.orangejelly.co.uk
NEXT_PUBLIC_BASE_URL=

# Example for development:
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Example for staging:
# NEXT_PUBLIC_BASE_URL=https://staging.orangejelly.co.uk

# Sanity CMS Configuration (Required for CMS functionality)
# Get these values from your Sanity project dashboard at https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=9brdfanc
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API Token (Required for write operations and preview mode)
# Create a token in your Sanity project: Settings > API > Tokens
# Use a token with "Editor" or "Viewer" permissions
SANITY_API_TOKEN=your-sanity-api-token-here

# Sanity Preview Secret (Required for preview mode)
# Generate a random string for security: openssl rand -base64 32
SANITY_PREVIEW_SECRET=your-random-preview-secret-here

# Google Tag Manager ID (Optional - for analytics)
# Get your GTM ID from https://tagmanager.google.com
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX